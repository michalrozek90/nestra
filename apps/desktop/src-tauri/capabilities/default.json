{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the hardened Nestra desktop shell",
  "windows": ["main"],
  "permissions": ["core:default", "allow-auth-secret-storage"]
}
