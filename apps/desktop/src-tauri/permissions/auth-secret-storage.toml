[[permission]]
identifier = "allow-auth-secret-storage"
description = "Allows the desktop client to store authentication secrets in the OS credential store"
commands.allow = [
  "get_auth_secret",
  "set_auth_secret",
  "delete_auth_secret",
  "clear_auth_secrets"
]
