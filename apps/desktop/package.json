{
  "name": "@nestra/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Tauri desktop shell that packages the Expo web client",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "node ./scripts/run-tauri.mjs dev",
    "build": "node ./scripts/run-tauri.mjs build -- --locked",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
