{
  "name": "@nestra/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Tauri desktop shell that packages the Expo web client",
  "scripts": {
    "dev": "node ./scripts/run-tauri.mjs dev",
    "build": "node ./scripts/run-tauri.mjs build",
    "tauri": "tauri",
    "validate:updater-manifest": "node ./scripts/validate-updater-manifest.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
